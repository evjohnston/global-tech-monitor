{
  "name": "gtm-live-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker — live proxy for EPO patents and NSF awards. Exists because EPO needs a secret the browser can't hold, and research.gov sends no CORS headers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260718.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.112.0"
  }
}
